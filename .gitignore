# Created by https://www.toptal.com/developers/gitignore/api/dotenv,linux
# Edit at https://www.toptal.com/developers/gitignore?templates=dotenv,linux

### dotenv ###
.env

### Linux ###
*~

# temporary files which can be created if a process still has a handle open of a deleted file
.fuse_hidden*

# KDE directory preferences
.directory

# Linux trash folder which might appear on any partition or disk
.Trash-*

# .nfs files are created when an open file is removed but is still being accessed
.nfs*

# End of https://www.toptal.com/developers/gitignore/api/dotenv,linux

# Git-ignored, per-repo mirrors of the global lesson stores (~/.claude/memory/lessons*).
# Local records only — the source of truth is the global store; excluded from any docs site.
docs/blueprintx-lessons.md
docs/dotfiles-dev-lessons.md
