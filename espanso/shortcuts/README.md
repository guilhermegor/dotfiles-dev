shortcuts espanso helper

This package provides a :shortcuts trigger that prints all espanso packages with descriptions and triggers.

Usage:
- Install packages: make install_espanso_packages
- In any GUI text field, type :shortcuts and press Space

Script:
- shortcuts.sh lists each package, its description and triggers from package.yml
